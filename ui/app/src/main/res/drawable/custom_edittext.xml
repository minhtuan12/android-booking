<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Thay đổi màu nền nếu cần -->
    <corners android:radius="6dp" /> <!-- Thay đổi radius để điều chỉnh độ tròn của góc -->
    <stroke android:width="1dp" android:color="#000000" /> <!-- Tạo viền nếu cần, thay đổi màu và độ rộng của viền -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>